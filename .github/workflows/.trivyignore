# Status: Ignored for now - action to be done: upgrade docker image gcr.io/distroless/java17-debian12
# Reason: ARLAS-tagger does not expose any XML parsing functionality to untrusted sources.
# Description: In libexpat before 2.7.4, the doContent function does not properly determine the buffer size bufSize because there is no integer overflow check for tag buffer reallocation.  https://avd.aquasec.com/nvd/2026/cve-2026-25210/
CVE-2026-25210