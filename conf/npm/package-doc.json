{
  "name": "arlas-tagger-api",
  "version": "23.0.3-SNAPSHOT",
  "license": "Apache 2.0",
  "scripts": {
    "build-release": "tsc -p tsconfig-build.json",
    "postbuild": "mkdir dist && mv *.js.map *.js *.d.ts ./dist/"
  },
  "private": true,
  "dependencies": {
    "es6-promise": "4.2.4",
    "isomorphic-fetch": "3.0.0",
    "url": "0.11.0"
  },
  "devDependencies": {
    "typescript": "^4.0.0"
  },
  "overrides": {
    "isomorphic-fetch": {
      "node-fetch": "3.3.0"
    }
  }
}